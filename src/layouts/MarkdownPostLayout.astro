---
const { frontmatter } = Astro.props;
---

<h1>{frontmatter.title}</h1>
<h2>Written by {frontmatter.author}</h2>
<h4>Published on: {frontmatter.pubDate.slice(0, 10)}</h4>
<p><em>{frontmatter.description}</em></p>
<slot />
